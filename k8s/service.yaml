apiVersion: v1
kind: Service
metadata:
  name: translation-service
  namespace: translation-app
spec:
  type: NodePort
  selector:
    app: translation-api
  ports:
  - port: 5001
    targetPort: 5001
    nodePort: 30001